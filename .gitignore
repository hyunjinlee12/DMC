# VASP large binary / intermediate outputs
WAVECAR
CHGCAR
CHG
PROCAR
PCDAT
EIGENVAL
DOSCAR
REPORT
XDATCAR
vasprun.xml
POTCAR

# VASP job logs
vasp_*.out
vasp_*.err

# Python
__pycache__/
*.pyc

# Claude Code internal
.claude/projects/
.claude/settings.local.json

# Large ASE trajectory dumps (regeneratable from pipeline scripts)
# (Per-surface SetA/SetB/SetTS/side/rejected are hundreds of MB; keep summary.json instead.)
calculations/G3_adsorption*/**/*.traj

# Large generated reports (regeneratable)
*.tar.gz

# povray temporary rendering files (scene script + ini regenerated by render_*.py)
calculations/**/*.pov
calculations/**/*.ini
